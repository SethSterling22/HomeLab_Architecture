{
  "name": "homelab-shelly-gen4-exporter",
  "version": "1.0.0",
  "description": "Minimal Prometheus exporter for Shelly Gen4 devices (the shellyctl image does not support Gen4's device spec yet) — polls the Gen2+ RPC Switch.GetStatus endpoint directly on each scrape.",
  "main": "shelly-gen4-exporter.js",
  "type": "commonjs",
  "scripts": {
    "start": "node shelly-gen4-exporter.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "private": true
}
